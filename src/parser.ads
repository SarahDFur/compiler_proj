package Parser is

   -- Will have a switch, in which all instructions 
   -- Instruction Line will be sent to the appropriate write_to_file functions
   procedure parse_Instruction (Line : String);

end Parser;
